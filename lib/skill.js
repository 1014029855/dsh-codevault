/**
 * The code-reading framework — dsh-codevault's core value.
 *
 * Registered via `ctx.skills.register()`. The model loads this body when the
 * user reads source code and wants to record it (or revisit the archive) and
 * should then write against this template and call the plugin's tools with the
 * conventions below. Content is Chinese by design (V1).
 */
export const SKILL_NAME = 'code-reading';
export const CODE_READING_SKILL = {
    name: SKILL_NAME,
    source: 'custom',
    description: '源码阅读档案（dsh-codevault）：读开源代码后快记或写"坐标式"深读笔记，沉淀为可回访的个人阅读档案（可放入 Obsidian）。',
    whenToUse: '用户提到正在读/刚读完某个开源仓库、文件、函数或机制的源码，想记录"读懂了什么"、写深读笔记、' +
        '回顾自己读过哪些代码，或说"记一下""刚才读到的地方记下来"时加载。',
    content: [
        '# 阅读框架（dsh-codevault）',
        '',
        '你不是在写读后感，而是在给这次阅读**建立坐标档案**：先定位（读了哪个仓库/文件/符号里的什么机制、它为什么长这样），',
        '再下结论（我因此多懂了什么、还能怎么用）。宁可少写形容词，也要写清楚坐标与机制。',
        '不要给代码打分或写情绪化"印象"——档案记的是**理解**，不是评价。',
        '',
        '## 记录的两档姿势',
        '',
        '- **快记（10 秒）**：用户只是说"记一下刚才读到的" → 调用 `read_capture`。note 写 2–4 句：' +
            '读到的是什么机制、关键点在哪、它为什么这样设计——不要只写一句空话，Obsidian 卡片的内容取决于你写多厚。',
        '- **深读笔记（几分钟）**：用户明确要"深读笔记 / 讲透这个机制 / 系统学这个模块" → 调用 `read_note`，正文按下方模板，中文、≥200 字。',
        '- **先记后深（推荐节奏）**：读的时候先 `read_capture` 快记保底；想深挖时再对那条快记调 `read_expand`，' +
            '把正文与 takeaway 原位补进同一张卡——不需要重新报坐标。用户说"把刚才那条展开/写详细点"时就这么做。',
        '',
        '## 对象卡（重要概念）',
        '',
        '- 档案按"阅读对象"组织成卡：同一个对象（repo + path + symbol 给出的最深坐标）的**多次阅读累积在同一张 Obsidian 卡**上，' +
            '卡片里有"阅读时间线（新→旧）"与"全部提炼要点（去重合并）"。再次读同一个地方 = 在时间线里追加，不是新建一张卡。',
        '- 所以**同一对象每次用同一套坐标**：仓库名大小写一致、path 用同一写法（如都带 src/ 前缀）、symbol 保持一致；' +
            '坐标写法漂移会把一个对象拆成两张卡。symbol 只在确定读到的符号时填，深浅不同的坐标 = 不同对象（如文件级 vs 文件里的符号级）。',
        '- 想让卡片标题更好读：在**第一次**记这个对象时给个精炼 title（它成为文件名并冻结），之后重读不需要重复 title。',
        '',
        '## 关联 vault 里的已有知识（配置了 vaultDir 时）',
        '',
        '- 插件可只读检索你的 Obsidian vault（`vault_search` / `vault_read`）。记一条阅读前，若用户 vault 里可能有相关笔记，' +
            '先用 `vault_search` 按机制名/主题词查一下；确实相关时，在快记 note 或深读笔记正文里用 `[[笔记名]]` 引用它。',
        '- 目的：让"读码档案"长进你更大的知识网（论文、项目、读书笔记），而不是孤岛。',
        '- 纪律：**只读不写**——vault 里其它笔记永远由你/Obsidian 维护，插件不改动 vault 内任何非插件文件；' +
            '没有把握相关就不要硬链。没配置 vaultDir 时跳过这一步。',
        '',
        '## 记录后补链（vault_suggest → 用户确认 → read_link）',
        '',
        '- 落盘后想看看有没有"更早写过、值得关联"的笔记：调 `vault_suggest`，给这个对象的关键词/tags，' +
            '它会按命中数排序返回候选（只读、只看用户笔记，不含档案卡）。',
        '- 把候选列给用户确认（别自作主张全链上）；用户点头后调 `read_link` 给事件补结构化链接' +
            '（去重、不改正文），该对象卡的"关联 vault 笔记"小节立即出现这些 [[笔记名]]。',
        '- 补链是事后增强：宁可少而准，没把握相关就不要硬链。',
        '- 工具返回里若带 `relatedNotes`（候选旧笔记）或 `suggestUpgrade`（建议升级深读），那是**建议不是已执行**：' +
            '把建议转述给用户，等他确认再调 `read_link` / `read_expand`。工具层从不自动改档案。',
        '',
        '## 回访问题（revisit）：让卡片"叫你回来想"',
        '',
        '- 每次记录都尽量给 2–3 条 `revisit` 问题，它们会汇聚到卡片末尾的"回访问题"小节。',
        '- 好问题的标准：**必须重新推导机制才能回答**（如"这个注入在插件生命周期的哪一步触发？换一种注册顺序会怎样？"），' +
            '而不是能直接搜到答案的琐碎事实（如"这个文件叫什么名"）。',
        '- 目的：档案不只是"存下来"，还要在你回看时触发一次主动回忆（提取练习比重复阅读记得牢）。' +
            '写不出好问题就不写，别硬凑。',
        '',
        '## 标签纪律（tags / newTags）',
        '',
        '- `tags` 只接受**档案里已存在的标签**（或本对象已用过的），工具会拒掉词表外的建议并把它们回传给你，' +
            '这样标签不会越积越乱；被拒的标签可以顺口告诉用户。',
        '- 唯一的例外是**空档案的种子期**：档案里一个标签都还没有时，第一批记录写入的标签会作为词表起点（否则永远无法建词表）。',
        '- 确实需要一个新的主题词（用户明确要、或新领域）时，放进 `newTags`——它会成为词表的一部分。',
        '- 标签是检索用的主题词，不要用标签表达评价；人工写过的标签永远保留（卡片标签是各次事件的并集）。',
        '',
        '## 回看历史与撤销（read_history / read_delete）',
        '',
        '- 用户问"我上次是怎么理解这个的 / 之前读到哪一步"时，用 `read_history` 回放该对象卡：' +
            '给 noteFile（卡片名）、id 或 repo(+path/symbol) 定位，`index` 回放到第 N 次阅读、`at` 回放到某日期之前；' +
            '它返回的正是当时那版卡片，可直接对比"现在的理解 vs 上次的理解"。',
        '- 想续读/重想时，先 `read_history` 看上次留下的"回访问题"，再决定这次记什么。',
        '- `read_delete` 只在用户**明确要求删掉某条记录**时用，且必须 `confirm=true`；' +
            '档案是只追加的，删除不可恢复，删前先把要删的内容向用户复述确认。',
        '',
        '## 快记的正文密度（重要）',
        '',
        '- 快记不等于"一句话"。note 至少写清三件事：**这是什么机制 → 关键设计/数据流是什么 → 为什么值得记住（或有什么坑）**。',
        '- 每条快记尽量给 title（一句话概括，成为文件名），没有好标题就用坐标自动命名。',
        '- 拿不准版本/符号就省略 ref/symbol，不要编造；宁可少而准。',
        '',
        '## 标题约定（title）',
        '',
        '- 调用工具时尽量给一个 **title**（≤120 字的内容概括，如"cordis 声明式依赖注入机制"）——它会成为 Obsidian 里笔记的文件名，可读性取决于它。',
        '- 快记的 title 可以很朴素："读懂了 X 的 Y 机制"；深读笔记给一句更像标题的概括。',
        '- title 省略时插件会用坐标（文件/符号）自动命名，所以**给了 title 才能得到好读的文件名**。',
        '',
        '## 深读笔记模板骨架',
        '',
        '1. **对象坐标**：repo@ref（版本/commit 拿不准就省略）；path；symbol；它处在什么位置——谁调用它、它调用谁、为什么存在。',
        '2. **机制**：它怎么工作——关键路径/数据流/数据结构，以及 1–2 个最值得记住的设计决定与取舍。',
        '3. **提炼与比较**：自己可复用的要点（写进 takeaway，至少 1 条）；与已知的另一种实现/标准库方案比较，差异点是什么。',
        '4. *可选* **存疑与回访**：没搞懂的、想验证的、待版本更新后复查的点——其中能变成"逼自己重新推导"的问句，放进 `revisit`。',
        '',
        '## 事实纪律（重要）',
        '',
        '- context（为什么读到这 / 当时的问题）对深读笔记是**必填**的：阅读动机是档案坐标的一部分。',
        '- repo/path/symbol 写准确；`ref`（版本/commit/分支）拿不准就省略，**不要编造**；symbol 只在确实读到该符号时填。',
        '- repo 支持 owner/name 或本地目录名；同一仓库尽量用同一写法（大小写一致），否则会分成两个 hub。',
        '- tags 是可选的自由主题词，自动小写；不要用 tags 表达评价。',
        '- 正文只放 `bodyMarkdown`；结构化字段不要塞评论。写完后用工具返回的 id/路径向用户做一句简短确认。',
    ].join('\n'),
};
//# sourceMappingURL=skill.js.map